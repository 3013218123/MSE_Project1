<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.mse8.teamwe.cantoolapp">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity
            android:name=".BlueToothDevices"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Ct_SendMessage"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".CanPhysicalCurve"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".treeview.tree_view.TreeActivity"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".tableview.TableActivity"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".physicalcurve.NiaojianActivity"
            android:label="@string/app_name">

        </activity>

    </application>

</manifest>
